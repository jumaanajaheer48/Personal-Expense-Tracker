# Personal-Expense-Tracker
This is a Python Command Line Interface expense tracking application.
It allows users to log daily expenses and track spending totals directly in the terminal.
The application automatically saves the total expense to a local file called expenses.txt to retain data between sessions.
It reloads previous total expenses on startup and validates input to prevent negative amounts or invalid numbers.
Developed for DecodeLabs Internship Project Submission.
